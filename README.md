# Auto-Encoder on Fashion-MNIST

This project implements a  **Autoencoder** in PyTorch using separate **Encoder** and **Decoder** modules.  
The autoencoder is trained on the **Fashion-MNIST** dataset to learn compressed representations of clothing images and reconstruct them.


